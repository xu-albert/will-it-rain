{
  "name": "will-it-rain-backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "npm run typecheck && npm run test:unit && npm run test:endpoints",
    "test:unit": "tsc src/validate.ts --outDir .test-build --module esnext --target es2022 --moduleResolution bundler && echo '{\"type\":\"module\"}' > .test-build/package.json && node test/validate.test.mjs",
    "test:endpoints": "./test/endpoints.test.sh"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250224.0",
    "wrangler": "^4.0.0",
    "typescript": "^5.7.0"
  }
}
